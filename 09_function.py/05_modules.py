import math 
import mymodule
print(math.sqrt(16))
# This are builtin module you can make by own also like example
mymodule.hi() 
#  While some are external modules like numpy , pandas to install that do 
#  pip install pandas